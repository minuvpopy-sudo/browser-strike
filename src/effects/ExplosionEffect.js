import * as THREE from 'three';
export class ExplosionEffect { constructor(scene){this.scene=scene;this.items=[];}spawn(position){const mesh=new THREE.Mesh(new THREE.SphereGeometry(1,10,8),new THREE.MeshBasicMaterial({color:0xff9a32,transparent:true,opacity:.8,wireframe:true}));mesh.position.copy(position);this.scene.add(mesh);this.items.push({mesh,life:.65});}update(dt){for(let i=this.items.length-1;i>=0;i--){const e=this.items[i];e.life-=dt;e.mesh.scale.addScalar(dt*14);e.mesh.material.opacity=Math.max(0,e.life/.65);if(e.life<=0){this.scene.remove(e.mesh);e.mesh.geometry.dispose();e.mesh.material.dispose();this.items.splice(i,1);}}}}
