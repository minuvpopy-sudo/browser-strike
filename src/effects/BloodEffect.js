import * as THREE from 'three';
export class BloodEffect { constructor(scene){this.scene=scene;this.particles=[];}spawn(point){for(let i=0;i<6;i++){const m=new THREE.Mesh(new THREE.BoxGeometry(.025,.025,.025),new THREE.MeshBasicMaterial({color:0x6f1713}));m.position.copy(point);this.scene.add(m);this.particles.push({m,v:new THREE.Vector3((Math.random()-.5)*2,Math.random()*1.7,(Math.random()-.5)*2),life:.45});}}update(dt){for(let i=this.particles.length-1;i>=0;i--){const p=this.particles[i];p.v.y-=7*dt;p.m.position.addScaledVector(p.v,dt);p.m.material.opacity=p.life/.45;if((p.life-=dt)<=0){this.scene.remove(p.m);p.m.geometry.dispose();p.m.material.dispose();this.particles.splice(i,1);}}}}
